# Q2.1: Enhanced Intent Layer - Architecture Specification (Corrected)

## Core Purpose

Q2.1 is the **critical intelligence module** that analyzes question intent with enhanced semantic categories. Unlike traditional intent classification, Q2.1 focuses on **analytical operations**, **temporal relationships**, and **computational requirements** that guide the Q-Pipeline's geometric matching system.

## Key Innovation: Question Semantic Intent Categories

Q2.1 introduces revolutionary intent categories that enable the Q-Pipeline to understand **what type of information processing** questions require, independent of document structure which is handled by the A-Pipeline.

### Traditional vs Enhanced Intent Classification

#### Traditional Intent Categories (B-Pipeline)
```python
traditional_intents = {
    "comparison": float,     # General comparison intent
    "calculation": float,    # Basic calculation need
    "definition": float,     # Definition seeking
    "identification": float, # Entity identification
    "factual": float        # Factual information
}
```

#### Enhanced Intent Categories (Q2.1)
```python
enhanced_intents = {
    # Existing categories (preserved)
    "comparison": float,
    "calculation": float,
    "definition": float,
    "identification": float,
    "factual": float,

    # NEW: Question Semantic Intent Categories
    "temporal_lookup": float,        # Year/date specific queries
    "numerical_extraction": float,   # Direct numeric value retrieval
    "analytical_operation": float,   # Calculation/percentage/ratio operations
    "contextual_integration": float, # Multi-element context synthesis
    "aggregation": float,           # Sum/average/count operations
    "comparison_temporal": float,    # Year-over-year comparisons
    "trend_analysis": float,        # Pattern/trend identification
    "computational_requirement": float, # Complex computation needed
}
```

## Input/Output Specification

### Input File
- **Source**: `Q_Question_Pipeline/outputs/Q1_Question_ingestion.json`
- **Required Fields**:
  - `question_id`: Unique question identifier
  - `question_text`: Raw question text for analysis
  - `doc_id`: Document reference (for context-aware classification)

### Output File
- **Target**: `Q_Question_Pipeline/outputs/Q2.1_enhanced_intent_classification.json`
- **Contains**: Enhanced intent vectors with confidence scores and semantic analysis metadata

### Output Structure
```python
{
    "question_id": {
        "question_id": str,
        "doc_id": str,
        "question_text": str,
        "intent_classification": {
            # Traditional intents
            "comparison": float,
            "calculation": float,
            "definition": float,
            "identification": float,
            "factual": float,

            # Enhanced question semantic intents
            "temporal_lookup": float,
            "numerical_extraction": float,
            "analytical_operation": float,
            "contextual_integration": float,
            "aggregation": float,
            "comparison_temporal": float,
            "trend_analysis": float,
            "computational_requirement": float
        },
        "intent_vector": List[float],  # 13-dimensional intent vector
        "primary_intent": str,         # Highest scoring intent category
        "intent_confidence": float,    # Confidence in classification (0-1)
        "question_semantic_indicators": {
            "temporal_markers": List[str],      # Detected time references
            "numerical_patterns": List[str],    # Detected number patterns
            "calculation_keywords": List[str],  # Calculation operation words
            "comparison_indicators": List[str], # Comparison signal words
            "computational_signals": List[str] # Complex computation indicators
        },
        "processing_metadata": {
            "classification_timestamp": str,
            "processing_time_ms": float,
            "pattern_matches": int,
            "confidence_factors": List[str]
        }
    }
}
```

## Core Architecture Components

### 1. Question Semantic Pattern Engine

```python
class QuestionSemanticPatternEngine:
    """
    Advanced pattern recognition for question semantic analysis
    """

    def __init__(self):
        self.temporal_patterns = self._load_temporal_patterns()
        self.analytical_patterns = self._load_analytical_patterns()
        self.computational_patterns = self._load_computational_patterns()

    def analyze_question_semantics(self, question_text: str) -> Dict:
        """
        Analyze question semantic patterns for intent classification
        """
        return {
            'temporal_lookup_score': self._detect_temporal_requirements(question_text),
            'analytical_operation_score': self._detect_analytical_operations(question_text),
            'computational_requirement_score': self._detect_computational_needs(question_text),
            'numerical_extraction_score': self._detect_numerical_needs(question_text)
        }
```

### 2. Enhanced Intent Classifier

```python
class EnhancedIntentClassifier:
    """
    Multi-dimensional intent classification focused on question semantics
    """

    def classify_enhanced_intent(self, question_text: str, doc_id: str) -> Dict:
        """
        Perform enhanced intent classification with 13 categories

        Returns:
            Dict containing all intent scores and metadata
        """
        # Semantic pattern analysis
        semantic_patterns = self.semantic_engine.analyze_question_semantics(question_text)

        # Traditional intent analysis
        traditional = self.classify_traditional_intent(question_text)

        # Enhanced intent synthesis
        enhanced = self.synthesize_enhanced_intents(semantic_patterns, traditional)

        return {
            'intent_scores': enhanced,
            'primary_intent': max(enhanced, key=enhanced.get),
            'confidence': self.calculate_confidence(enhanced, semantic_patterns)
        }
```

### 3. Temporal Analysis Engine

```python
class TemporalAnalysisEngine:
    """
    Analyzes temporal intent in questions (independent of document structure)
    """

    def __init__(self):
        self.year_patterns = [r'\b(19|20)\d{2}\b', r'\b\d{4}\b']
        self.period_patterns = [
            r'quarter', r'q[1-4]', r'fiscal year', r'annual', r'yearly',
            r'month', r'weekly', r'daily', r'period'
        ]
        self.comparison_patterns = [
            r'from .+ to', r'between .+ and', r'year.over.year', r'yoy',
            r'compared to', r'versus', r'change', r'growth', r'increase', r'decrease'
        ]

    def analyze_temporal_intent(self, question_text: str) -> Dict:
        """
        Analyze temporal requirements in question semantics
        """
        text_lower = question_text.lower()

        # Year detection
        years = []
        for pattern in self.year_patterns:
            years.extend(re.findall(pattern, question_text))

        # Period detection
        periods = [p for p in self.period_patterns if re.search(p, text_lower)]

        # Comparison detection
        comparisons = [c for c in self.comparison_patterns if re.search(c, text_lower)]

        return {
            'temporal_lookup_score': self._calculate_temporal_score(years, periods),
            'comparison_temporal_score': self._calculate_comparison_score(comparisons, years),
            'detected_years': years,
            'detected_periods': periods,
            'comparison_indicators': comparisons
        }

    def _calculate_temporal_score(self, years: List[str], periods: List[str]) -> float:
        """Calculate temporal lookup score"""
        # High score if multiple years mentioned
        year_score = min(1.0, len(years) * 0.4)
        period_score = min(0.3, len(periods) * 0.1)
        return min(1.0, year_score + period_score)

    def _calculate_comparison_score(self, comparisons: List[str], years: List[str]) -> float:
        """Calculate temporal comparison score"""
        # High score if comparison terms + multiple years
        comparison_score = min(0.7, len(comparisons) * 0.3)
        year_bonus = 0.3 if len(years) >= 2 else 0.0
        return min(1.0, comparison_score + year_bonus)
```

### 4. Analytical Operation Detector

```python
class AnalyticalOperationDetector:
    """
    Detects analytical operations required by questions (semantic analysis)
    """

    def __init__(self):
        self.calculation_keywords = [
            'percentage', 'percent', '%', 'ratio', 'rate', 'change', 'growth',
            'increase', 'decrease', 'difference', 'total', 'sum', 'average',
            'mean', 'calculate', 'compute', 'derive', 'determine'
        ]
        self.aggregation_keywords = [
            'total', 'sum', 'aggregate', 'combined', 'overall', 'net',
            'gross', 'average', 'mean', 'maximum', 'minimum', 'count'
        ]
        self.trend_keywords = [
            'trend', 'pattern', 'growth', 'decline', 'trajectory', 'direction',
            'progression', 'evolution', 'development', 'over time'
        ]

    def detect_analytical_operations(self, question_text: str) -> Dict:
        """
        Detect analytical operation requirements in question semantics
        """
        text_lower = question_text.lower()

        # Operation type detection
        calc_matches = [kw for kw in self.calculation_keywords if kw in text_lower]
        agg_matches = [kw for kw in self.aggregation_keywords if kw in text_lower]
        trend_matches = [kw for kw in self.trend_keywords if kw in text_lower]

        return {
            'analytical_operation_score': min(1.0, len(calc_matches) * 0.3),
            'aggregation_score': min(1.0, len(agg_matches) * 0.25),
            'trend_analysis_score': min(1.0, len(trend_matches) * 0.2),
            'calculation_keywords': calc_matches,
            'aggregation_keywords': agg_matches,
            'trend_keywords': trend_matches
        }
```

### 5. Computational Requirement Analyzer

```python
class ComputationalRequirementAnalyzer:
    """
    Analyzes computational complexity requirements of questions
    """

    def __init__(self):
        self.computational_indicators = [
            'calculate', 'compute', 'derive', 'determine', 'find', 'what is',
            'how much', 'how many', 'what was', 'what will be'
        ]
        self.complex_operations = [
            'percentage change', 'compound growth', 'ratio analysis',
            'variance', 'correlation', 'regression', 'forecast'
        ]

    def analyze_computational_requirements(self, question_text: str) -> Dict:
        """
        Analyze computational complexity requirements
        """
        text_lower = question_text.lower()

        # Basic computational indicators
        basic_matches = [indicator for indicator in self.computational_indicators
                        if indicator in text_lower]

        # Complex operation indicators
        complex_matches = [op for op in self.complex_operations
                          if op in text_lower]

        computational_score = 0.0

        # Basic computation score
        if basic_matches:
            computational_score += 0.5

        # Complex computation bonus
        if complex_matches:
            computational_score += 0.5

        return {
            'computational_requirement_score': min(1.0, computational_score),
            'basic_computational_signals': basic_matches,
            'complex_computational_signals': complex_matches
        }
```

## Implementation Algorithm

### Step 1: Load Question Data
```python
def load_question_from_q1(question_id: str) -> Dict:
    """
    Load specific question from Q1 output
    """
    q1_path = "Q_Question_Pipeline/outputs/Q1_Question_ingestion.json"
    with open(q1_path, 'r') as f:
        q1_data = json.load(f)

    if isinstance(q1_data, dict) and 'question_id' in q1_data:
        # Single question format
        if q1_data['question_id'] == question_id:
            return q1_data
    elif isinstance(q1_data, dict):
        # Multi-question format
        return q1_data.get(question_id)

    raise ValueError(f"Question {question_id} not found in Q1 output")
```

### Step 2: Enhanced Intent Classification
```python
def classify_enhanced_intent(question_text: str, doc_id: str) -> Dict:
    """
    Perform 13-dimensional intent classification focused on question semantics
    """
    # Initialize all intent scores
    intent_scores = {
        # Traditional intents
        "comparison": 0.0,
        "calculation": 0.0,
        "definition": 0.0,
        "identification": 0.0,
        "factual": 0.0,

        # Enhanced question semantic intents
        "temporal_lookup": 0.0,
        "numerical_extraction": 0.0,
        "analytical_operation": 0.0,
        "contextual_integration": 0.0,
        "aggregation": 0.0,
        "comparison_temporal": 0.0,
        "trend_analysis": 0.0,
        "computational_requirement": 0.0
    }

    # Semantic pattern analysis
    temporal_analysis = self.temporal_engine.analyze_temporal_intent(question_text)
    analytical_analysis = self.analytical_detector.detect_analytical_operations(question_text)
    computational_analysis = self.computational_analyzer.analyze_computational_requirements(question_text)

    # Update scores based on semantic analysis
    intent_scores.update({
        "temporal_lookup": temporal_analysis['temporal_lookup_score'],
        "comparison_temporal": temporal_analysis['comparison_temporal_score'],
        "analytical_operation": analytical_analysis['analytical_operation_score'],
        "aggregation": analytical_analysis['aggregation_score'],
        "trend_analysis": analytical_analysis['trend_analysis_score'],
        "computational_requirement": computational_analysis['computational_requirement_score']
    })

    # Traditional intent analysis
    traditional_scores = self.analyze_traditional_intents(question_text)
    intent_scores.update(traditional_scores)

    # Numerical extraction score (based on question semantics)
    intent_scores["numerical_extraction"] = self._calculate_numerical_extraction_score(question_text)

    # Contextual integration score
    intent_scores["contextual_integration"] = self._calculate_contextual_integration_score(question_text)

    # Normalize scores
    normalized_scores = self.normalize_intent_scores(intent_scores)

    return {
        'intent_classification': normalized_scores,
        'intent_vector': list(normalized_scores.values()),
        'primary_intent': max(normalized_scores, key=normalized_scores.get),
        'intent_confidence': self.calculate_confidence(normalized_scores)
    }
```

### Step 3: Semantic Indicator Extraction
```python
def extract_semantic_indicators(question_text: str, analysis_results: Dict) -> Dict:
    """
    Extract semantic indicators from question analysis
    """
    return {
        'temporal_markers': analysis_results.get('detected_years', []) +
                           analysis_results.get('detected_periods', []),
        'numerical_patterns': self._extract_numerical_patterns(question_text),
        'calculation_keywords': analysis_results.get('calculation_keywords', []),
        'comparison_indicators': analysis_results.get('comparison_indicators', []),
        'computational_signals': analysis_results.get('basic_computational_signals', []) +
                               analysis_results.get('complex_computational_signals', [])
    }
```

## Integration Points

### Input Dependencies
- **Q1 Output**: Requires `Q1_Question_ingestion.json` with question_id and question_text
- **Question Context**: Uses question semantics for classification (not document structure)

### Output Consumers
- **Q2.3**: Uses semantic intent indicators for document structure alignment
- **Q2.5**: Uses intent_vector for coordinate calculation weighting (30% contribution)
- **Q3.1**: Uses primary_intent for strategy weighting in matching

## Performance Targets

### Processing Metrics
- **Processing Speed**: <50ms per question
- **Classification Accuracy**: >85% for question semantic intents
- **Memory Usage**: <50MB for pattern libraries
- **Confidence Threshold**: Average >0.7 across test set

### Quality Indicators
- **Temporal Classification**: >90% accuracy for time-based question semantics
- **Analytical Operations**: >85% accuracy for calculation question requirements
- **Computational Requirements**: >80% accuracy for complexity assessment

## Success Criteria

### Functional Requirements
1. **13-Dimensional Classification**: All intent categories properly scored based on question semantics
2. **Semantic Pattern Recognition**: Accurate detection of question requirements
3. **Confidence Scoring**: Reliable confidence estimation
4. **Integration Ready**: Seamless data flow to Q2.3 and Q2.5

### Quality Requirements
1. **High Precision**: >85% correct primary intent classification
2. **Question Focus**: Superior performance on question semantic analysis (not document structure)
3. **Processing Speed**: Real-time classification capability
4. **Robust Error Handling**: Graceful handling of edge cases

## Configuration Parameters

```python
q21_config = {
    "confidence_threshold": 0.7,        # Minimum confidence for classification
    "temporal_analysis_weight": 0.3,    # Weight for temporal semantic patterns
    "analytical_operation_weight": 0.4, # Weight for analytical semantic patterns
    "computational_weight": 0.3,        # Weight for computational complexity patterns
    "pattern_cache_size": 1000,         # Pattern matching cache size
    "processing_timeout": 100           # Max processing time (ms)
}
```

## Summary

Q2.1 Enhanced Intent Layer is the **critical intelligence module** that enables the Q-Pipeline to understand question semantic requirements, focusing purely on **what type of information processing** questions need. By providing enhanced intent classification based on analytical operations, temporal relationships, and computational requirements, Q2.1 supplies essential semantic vectors that guide downstream geometric processing.

**Key Innovation**: Revolutionary question semantic analysis that focuses on **what questions require** rather than **how documents are structured**, enabling proper separation of concerns with A-Pipeline handling document structure detection.

**Architectural Clarity**: Q2.1 analyzes question semantics, A-Pipeline detects document structure, Q2.3 aligns the two for optimal geometric matching.

---
*Q2.1 Enhanced Intent Layer Architecture v1.1 (Corrected)*
*Question Semantic Intelligence Module for Geometric Question Processing*